# share/use

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build share/use` to build the library.

## Running unit tests

Run `nx test share/use` to execute the unit tests via [Jest](https://jestjs.io).
