# share/functions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build share/functions` to build the library.

## Running unit tests

Run `nx test share/functions` to execute the unit tests via [Jest](https://jestjs.io).
