# useUser

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build useUser` to build the library.

## Running unit tests

Run `nx test useUser` to execute the unit tests via [Jest](https://jestjs.io).
