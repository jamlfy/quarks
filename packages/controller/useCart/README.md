# useCart

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build useCart` to build the library.

## Running unit tests

Run `nx test useCart` to execute the unit tests via [Jest](https://jestjs.io).
