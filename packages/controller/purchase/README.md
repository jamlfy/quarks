# purchase

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build purchase` to build the library.

## Running unit tests

Run `nx test purchase` to execute the unit tests via [Jest](https://jestjs.io).
