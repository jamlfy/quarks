# product/data

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build product/data` to build the library.

## Running unit tests

Run `nx test product/data` to execute the unit tests via [Jest](https://jestjs.io).
