# store/use

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build store/use` to build the library.

## Running unit tests

Run `nx test store/use` to execute the unit tests via [Jest](https://jestjs.io).
